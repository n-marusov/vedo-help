# Project Rules for /aif-implement

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-07-10 16:02
> Based on: 39 analyzed patches

## Rules

### Respect Task Phase Boundaries in Tests and Implementation
**Source**: 2026-06-16-15.00.md
**Rule**: Before implementing or adjusting tests, confirm the exact task contract and phase boundary. Do not assert integrated behavior for a standalone UI atom until the integration task exists. Use preview routes/components for standalone UI atoms when they need E2E verification before integration.

### Use Project Docker Service Names in Implementation
**Source**: 2026-06-18-19.20.md, 2026-06-18-00.50.md
**Rule**: When implementing Docker Compose or proxy configuration, set container-internal URLs explicitly through environment variables using Compose service names and container ports. Keep `localhost` only for host-local defaults, browser/public URLs, host-published ports, or self-healthchecks.

### Preserve Display Name vs Storage/API Identifier Separation
**Source**: 2026-06-18-19.20.md
**Rule**: When implementing APIs backed by systems with naming restrictions, separate user-visible display names from backend-safe identifiers. For Chroma collections in this project, use the collection UUID as the Chroma name and store the display name only in SQLite/UI-facing data.

### Implement SQLite UUID TEXT Row DTOs
**Source**: 2026-06-19-01.30.md
**Rule**: When implementing SQLite repositories with UUID `TEXT` columns, create internal row DTOs with `String` UUID fields and `TryFrom` conversions into domain structs. Add repository-level regression tests for UUID-rich query results.

### Implement Teleport Overlays With Viewport Positioning
**Source**: 2026-06-19-01.45.md
**Rule**: When implementing Vue components that `Teleport` menus/dropdowns to `body`, compute `trigger.getBoundingClientRect()` on open and position the overlay with fixed viewport coordinates. Refresh the position on resize, scroll, and option/content changes while open.

### Bind PostgreSQL UUID Columns as `uuid::Uuid`
**Source**: 2026-06-21-16.15.md, 2026-06-21-16.48.md
**Rule**: When implementing sqlx queries against PostgreSQL `uuid` columns, bind `uuid::Uuid` directly — never `String`. Parse strings with `Uuid::parse_str()` before binding. PostgreSQL has no implicit `text → uuid` cast in `=` / `IN` comparisons; stringifying a UUID causes `operator does not exist: uuid = text`. Where possible, prefer `sqlx::query!()` / `query_as!()` macros — their compile-time type checking catches `uuid = text` mismatches before runtime. For dynamic queries, use `Uuid::parse_str()` explicitly before binding.

### Validation Ordering: Global Checks Before Per-Item
**Source**: 2026-06-21-14.55.md
**Rule**: Implement validation in cheapest-first order: entry count and size limits before per-file MIME/content validation. A 10-file limit check must run before per-file ZIP entry extraction and validation.

### Vue Template Ordering: Loading Before Empty
**Source**: 2026-06-21-15.57.md
**Rule**: In `v-if`/`v-else-if` chains for async data, place the loading state check (`isLoading`) before the empty/fallback state check. The skeleton/loader must be the first condition in the chain.

### Pinia Setup Store Watch: `{ immediate: true }`
**Source**: 2026-06-21-15.57.md
**Rule**: For Pinia setup stores where `watch()` drives side effects like data fetching, include `{ immediate: true }` to fire the effect on mount. Direct mutation of `pinia.state.value` does not trigger computed getters — always use store actions to change state that has watchers.

### Use `ref()` for Shared API Client State
**Source**: 2026-06-26-16.48.md
**Rule**: When implementing module-level state that both the API client and reactive components access, use `ref()` from Vue, not plain `let`/`const` variables. Computed properties depending on non-reactive state will never re-evaluate.

### Validate API Response Shapes After Parsing
**Source**: 2026-06-28-02.52.md
**Rule**: Always add `Array.isArray()` guard after `JSON.parse()` before accessing `.length`. Provide fallback default values (e.g., `[]`) for nullable API response fields that may be `null` at runtime.

### SSE Streaming Pipeline Implementation
**Source**: 2026-07-09-00.22.md, 2026-07-10-00.24.md, 2026-07-10-01.08.md, 2026-07-10-01.18.md, 2026-07-10-01.25.md, 2026-07-10-10.28.md
**Rule**: When implementing SSE streaming or recovery:
- Persist user-visible state (messages, session title) before starting slow external work (embedding, search, LLM)
- Implement a fallback recovery UI indicator independent of temporary streaming placeholder messages
- Derive post-completion side effects from the pipeline's stable session ID, not transient UI selection state
- Use shared helper functions to keep reload/recovery completion behavior in sync with the normal streaming path
- For long-running background streams, implement a server-side supersession guard (client-side abort alone is insufficient)
- Edit/regenerate flows must preserve or explicitly replace a turn, not mix both strategies

### API Contract Design for Resource-Creating Endpoints
**Source**: 2026-07-10-12.13.md, 2026-07-10-16.02.md
**Rule**: When implementing endpoints that create resources with an update/resend variant:
- Accept an optional `existing_<entity>_id` parameter to support idempotent and resend scenarios
- Define explicit write coordination between endpoints that target the same entity table
- Express optional handler modes (create vs reuse) in the request schema — a developer reading the handler should not have to guess
- Document which party owns canonical identifiers (frontend temp IDs vs backend UUIDs) in the API contract
