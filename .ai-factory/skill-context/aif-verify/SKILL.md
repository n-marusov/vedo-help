# Project Rules for /aif-verify

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-07-10 16:02
> Based on: 39 analyzed patches

## Rules

### Verify Project Post-Implementation Checklist
**Source**: codebase convention, CHECKLIST.md
**Rule**: After any implementation verification in this project, read `CHECKLIST.md` and verify its gates, especially `npm run ai:validate`, before marking work complete. Report any skipped gate explicitly.

### Verify Docker Rendered Config
**Source**: 2026-06-18-19.20.md, 2026-06-18-15.20.md, 2026-06-17-02.58.md
**Rule**: For Docker/network/auth changes, run or request `docker compose config` and verify rendered service URLs, DNS entries, and ports match the intended browser/public, host-local, Docker-internal, and production exposure scopes.

### Verify E2E Infrastructure Before Interpreting Failures
**Source**: 2026-06-16-15.00.md, 2026-06-18-14.58.md
**Rule**: Before classifying Playwright failures as product defects, verify `webServer` configuration, browser installation, mocked endpoint URLs, store preconditions, and conditional element rendering waits.

### Verify SQLite UUID Round-Trip Regressions
**Source**: 2026-06-19-01.30.md
**Rule**: For Rust/`sqlx` SQLite changes touching UUID fields, verify tests cover `TEXT` UUID row decoding through `String` row DTOs and `Uuid::parse_str()` conversion.

### Verify PostgreSQL UUID Bind Regression Tests
**Source**: 2026-06-21-16.15.md, 2026-06-21-16.48.md
**Rule**: For Rust/sqlx PostgreSQL changes touching `uuid` columns, verify that repository-level regression tests exist where String parameters are parsed to `uuid::Uuid` before binding. Verify the test exercises the exact public API path (String in, Uuid out) that the query/repository method exposes.

### Verify Compose Profile Port Distinctness
**Source**: 2026-06-21-13.05.md
**Rule**: After Docker Compose changes, verify that port defaults across all profiles (base `docker-compose.yml`, `docker-compose.override.yml`, `docker-compose.test.yml`, `docker-compose.production.yml`) do not overlap. Run `docker compose config` for each profile and compare published host ports.

### Verify DB State After Data-Creating Fixes
**Source**: 2026-07-10-00.24.md, 2026-07-10-16.02.md
**Rule**: When verifying fixes that create or modify persisted data, check DB state via GET API response, not only rendered UI. A fix that masks the UI symptom but still persists duplicate or inconsistent data in the database is incomplete. Verify the write-path (no duplicate created), not only the read-path (no duplicate displayed).
