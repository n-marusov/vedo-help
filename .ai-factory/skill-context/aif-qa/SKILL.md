# Project Rules for /aif-qa

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-06-28 02:52
> Based on: 25 analyzed patches

## Rules

### QA E2E Contract Alignment
**Source**: 2026-06-16-15.00.md
**Rule**: When writing QA plans for UI atoms, distinguish standalone component contracts from later integrated chat/message behavior. Include preview-route verification when the component is not yet integrated.

### QA Playwright Preconditions
**Source**: 2026-06-18-14.58.md
**Rule**: For Playwright QA scenarios, specify required app/store state, conditional DOM waits, exact mocked endpoints, browser projects that must be installed, and the expected authenticated vs unauthenticated UI state.

### QA Custom Component Interaction
**Source**: 2026-06-18-14.58.md, 2026-06-19-01.45.md
**Rule**: For custom UI components such as `VSelect`, QA cases must interact with the actual rendered DOM, including teleported options, rather than assuming native `select`/`option` structure.

### QA Docker/Auth Smoke Scenarios
**Source**: 2026-06-18-19.20.md, 2026-06-18-15.20.md
**Rule**: For Docker/auth changes, include smoke scenarios that render `docker compose config`, verify host access for services required by host-run processes, verify container-internal service URLs, and exercise an authenticated protected endpoint.

### Database Tests: Require `#[serial]` or `--test-threads=1`
**Source**: 2026-06-21-02.40.md, 2026-06-21-15.57.md
**Rule**: For QA scenarios covering database integration tests, include `#[serial_test::serial]` (Rust) or `--test-threads=1` in test setup. TRUNCATE operations in shared databases will race under parallel execution, causing FK violations and data leakage between tests.

### Playwright Route Registration Ordering
**Source**: 2026-06-21-14.55.md, 2026-06-21-15.57.md
**Rule**: In Playwright QA scenarios that use `page.route()` for mock/intercept, ensure routes are registered before any action (goto, click, navigation, store mutation) that could trigger the request. For API response body consumption that happens in-page (blob, fetch), use `request.fetch()` directly instead of `page.waitForResponse()`.

### LLM-Dependent Test Timeouts and Propagation Waits
**Source**: 2026-06-21-14.55.md
**Rule**: For QA scenarios involving LLM responses (when using OpenRouter or other remote/slow LLM providers), recommend wait timeouts of 20-30s. Include Chroma propagation waits (minimum 2s) after data mutations before querying the vector DB. For local models or fast providers, shorter timeouts may suffice — adjust based on expected latency.

### Optimistic UI Regression Test Coverage
**Source**: 2026-06-21-14.41.md
**Rule**: For features with streaming/SSE and optimistic updates, include QA scenarios for the reconciliation window: test that edit/delete controls are hidden or disabled for pending messages, and that no API calls to UUID-restricted endpoints occur with temporary (non-persisted) message IDs.

### Default State Verification for Persisted UI
**Source**: 2026-06-26-18.16.md
**Rule**: For UI components with localStorage-persisted state (sidebar collapse, settings), include QA scenarios verifying the component's default state on fresh page load matches the design spec, not just a persisted-state recovery. Toggle buttons must visually reflect the current state.

### Track E2E Page Errors via `page.on('pageerror')`
**Source**: 2026-06-28-02.52.md
**Rule**: In Playwright QA scenarios involving session switching, navigation, or data loading, use `page.on('pageerror', ...)` to collect unhandled JavaScript errors during the test. Assert zero `pageerror` events after the action completes to catch unhandled exceptions that break reactivity or silently fail.
