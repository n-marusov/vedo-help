# Project Rules for /aif-plan

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-06-28 02:52
> Based on: 25 analyzed patches

## Rules

### Plan E2E Setup for Standalone UI Atoms
**Source**: 2026-06-16-15.00.md
**Rule**: When planning standalone UI atom work that requires E2E verification before integration, include a preview route or preview view task and Playwright `webServer` configuration if the tests depend on a local Vite server.

### Plan Exact E2E Preconditions
**Source**: 2026-06-18-14.58.md
**Rule**: For Vue/Playwright tasks, plan test preconditions explicitly: Pinia/store state, active collection IDs, conditional rendering waits, exact endpoint URLs, custom component DOM behavior, and browser project installation requirements.

### Plan Docker Network Scope Explicitly
**Source**: 2026-06-18-19.20.md, 2026-06-18-00.50.md, 2026-06-18-15.20.md
**Rule**: For Docker/auth/proxy tasks, include a URL scope matrix in the plan: browser/public, host-local, and Docker-internal. Specify which compose file owns each port or environment variable and which production overlay closes exposed ports.

### Plan Database Row Mapping Tests for SQLite UUIDs
**Source**: 2026-06-19-01.30.md
**Rule**: For Rust/`sqlx` SQLite features that add UUID-rich tables or queries, include repository-level tests proving `TEXT` UUID values round-trip through row DTO parsing into domain models.

### Plan Distinct Port Ranges per Compose Profile
**Source**: 2026-06-21-13.05.md
**Rule**: When planning Docker Compose profiles, ensure every host-published port has a default outside the range used by the default/dev stack. Audit `docker-compose.override.yml` and `<profile>.yml` together — any shared default port number is a collision risk. Follow the project's `18xxx` pattern but offset each profile (dev: 18001, test: 18081, etc.).

### Plan Chroma Eventual Consistency in Test Scenarios
**Source**: 2026-06-19-16.55.md, 2026-06-21-14.55.md
**Rule**: For RAG pipeline tests involving Chroma, plan for eventual consistency: include retry loops (up to 3 attempts, 500ms apart) and/or explicit propagation waits (minimum 2s) after data mutations before querying.

### Plan Ample Timeouts for LLM-Dependent E2E Tests
**Source**: 2026-06-21-14.55.md
**Rule**: For E2E tests involving LLM streaming (OpenRouter + Chroma retry loop), plan wait/assert timeouts of 20-30s, not 15s. Include explicit propagation waits after Chroma data mutations.

### Plan Regression Tests for Optimistic UI Windows
**Source**: 2026-06-21-14.41.md
**Rule**: When planning features with streaming/SSE completion and optimistic UI updates, include regression tests for the reconciliation window: pending message edit/delete controls should be hidden or disabled, and no API calls to UUID-restricted endpoints should occur with temporary/non-persisted IDs.

### Plan Default-State Verification for Persisted UI State
**Source**: 2026-06-26-18.16.md
**Rule**: For UI features with localStorage-persisted state, plan tests that verify the default state matches the design spec on fresh page load. Defaults should be design-first, not localStorage-first. A toggle button should always visually reflect the current state (icon, tooltip).
